import { NumOfActiveTitlePipe } from './num-of-active-title.pipe';

describe('NumOfActiveTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new NumOfActiveTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
